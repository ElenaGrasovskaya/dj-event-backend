{
    "routes": [
        {
            "method": "GET",
            "path": "api/events/me",
            "handler": "events.me",
            "config":{
                "policies": []
            }
        }
    ]
}